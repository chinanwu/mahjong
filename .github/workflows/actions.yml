name: Mahjong Helper actions
on: [workflow_dispatch]
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: npm i
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run unit tests
        run: npm t
